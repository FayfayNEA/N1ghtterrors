<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>BUY BUY BUY... its the only WAY</title>
        <link href="/css/styles.css" rel="stylesheet" type="text/css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Fragment+Mono&family=Kumbh+Sans:wght@300&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://use.typekit.net/art4fxf.css">
        <link rel="icon" type="image/png" href="/images/buckingbronc.png">
    </head>
    <body>
        <div class="top">
            <div class="bar">
                    <div class="shopping">
                        <button class="cart"> 
                            <!--<img src="/images/basket-cart-icon-27.png" alt="cart" width="80" height="80"/> --> 
                            <p class="word4cart">cart</p>
                            <div class="cartcounter" id="cartCounter">0</div>
                        </button>
                    </div>
                </div>
                <div class="card"> 
                    <div class="closeShopping">
                        <button class="closingX"> back to clothes. </button>
                    </div>
                    <div class="cartItems">
                    </div>
                    <div class="totalCART">
                        <p class="totalTitle"> total price: </p>
                        <span class="totalPrice"> $0 </span>
                    </div>
                    
                    <button class="checkOut" type="button" id="checkout-button">
                        checkout
                        <svg class="square-outline" viewBox="0 0 100 100" aria-hidden="true">
                            <line x1="1" y1="70" x2="300" y2="70" />
                            <line x1="115" y1="500" x2="115" y2="2" />
                    </svg>
                    </button>
                </div>
            
            <div class="video-container">
                <video autoplay muted loop playsinline webkit-playsinline poster="/images/videc.jpg" id="myVideo"></video>
                    <div class="menu-container">
                        <div class="menu-bar">
                        </div>
                    </div>
                    <source src="/images/shirts.mp4" type="video/mp4">
                </video>
            </div>

            

            <div class="scroll-spacer"></div>
            <div class="titleName" id="titleElement">
            <div class="titleText">N1GHTTERRORS</div>
            </div>
            
            <div class="diagonal-menu" id="diagonalMenu">
                <div class="menu-text">MENU</div>
                <div class="video-menu">
                    <a href="contactMe.html">Contact, Worries, Anixety</a>
                    <a href="whosthisfreak.html">A much due explanation</a>
                    <a href="portfolio.html">My other work as a Shiet PDF</a>
                </div> 
            </div>

            <button class="shirt"> 
                <img src="/images/buckingbronc.png" alt="shirt" width="30" height="30"/>
                <p class="lyrics"> STARE INTO THE VOID UNTIL THE VOID STARES BACK. </p>
            </button>
            <div class="content">  
                <span class="flex">
                    <div class="holder">  
                        <div class="gallery">
                            <img src="/images/buckingbronc.png" onclick="myFunction(this)"/>
                            <img src="/images/shirt1.png" onclick="myFunction(this)"/>
                            <img src="/images/post.jpg" onclick="myFunction(this)"/>
                            <img src="/images/shirt4.w.jpg" onclick="myFunction(this)"/>
                        </div>
                    </div>
                    <div class="bigimage">
                        <img id="imageBox" src="/images/shirt1.png" />
                    </div>
                    <div class="texts">
                        <img class="random" src="/images/buckingbronc.png" hidden/>
                        <span class="title">White collared baby. *wah wah* I know your daddy is in jail.</span>
                        <p> this shirt will make you cry. die. all the amazing feelings YOU need. Why??? cause its cute and goes with everything. Duah. style it with a skirt, jeans, maybe a tankie? you'll thank me later.</p>
                        <p class="size"> made for XL MEN. but gender isn't real so do with that as you will.</p>
                        <span class="thingPrice"> 35 </span>
                        <button class="checkout"> ADD TO BAG </button>
                    </div>
                </span>
            </div>

            <button class="shirt"> 
                <img src="/images/sumbmial man.png" alt="shirt" width="30" height="40"/>
                <p class="lyrics"> I'M SACRED OF WHAT HAPPENS WHEN IDEAS CONSUME YOU.</p>
            </button>
            <div class="content">  
                <span class="flex">
                    <div class="holder">  
                        <div class="gallery">
                            <img src="/images/sumbmial man.png" onclick="myFunction1(this)"/>
                            <img src="/images/Black3.jpg" onclick="myFunction1(this)"/>
                            <img src="/images/black2.jpg" onclick="myFunction1(this)"/>
                            <img src="/images/black4.jpg" onclick="myFunction1(this)"/>
                        </div>
                    </div>
                    <div class="bigimage">
                        <img id="imageBox1" src="/images/Black3.jpg" />
                    </div>
                    <div class="texts">
                        <img class="random" src="/images/sumbmial man.png" hidden/>
                        <span class="title">omg Mr.Rorschach please, PLEASE psychoanalyze me.</span>
                        <p> now UK u want a shirt with mustache legend Mr.Rorschach on the front. Now on the back? Well you tell me what YOU see, maybe you're insane, crazy or just like sooo quirky...</p>
                        <p class="size"> made for L MEN. but gender isn't real so do with that as you will.</p>
                        <span class="thingPrice"> 25 </span>
                        <button class="checkout"> ADD TO BAG </button>
                    </div>
                </span>
            </div>

            <button class="shirt"> 
                <img src="/images/virginmary.png" alt="shirt" width="30" height="40"/>
                <p class="lyrics">  WHEN I'M AFRAID I LOOSE MY MIND.</p>
            </button>
            <div class="content">  
                <span class="flex">
                    <div class="holder">  
                        <div class="gallery">
                            <img src="/images/virginmary.png" onclick="myFunction2(this)"/>
                            <img src="/images/nary1.jpg" onclick="myFunction2(this)"/>
                            <img src="/images/mary2.jpg" onclick="myFunction2(this)"/>
                            <img src="/images/mary11.jpg" onclick="myFunction2(this)"/>
                        </div>
                    </div>
                    <div class="bigimage">
                        <img id="imageBox2" src="/images/nary1.jpg" />
                    </div>
                    <div class="texts">
                        <img class="random" src="/images/virginmary.png" hidden/>
                        <span class="title">Classical art? Classical Architecure? the VIRGIN MARY WITH JESUS!?!?!?</span>
                        <p> don't sit here and pretend classical art wasn't steamy and SOOOO sexi. This shirt will make you as hot and bothered as St. Teresa sculpture. <br>
                            [bonus peom on back, a kissy from God if you can read it. DM and ill tell Them of ur virtuous deed...]
                        </p>
                        <p class="size"> made for L MEN. but gender isn't real so do with that as you will.</p>
                        <span class="thingPrice"> 25 </span>
                        <button class="checkout"> ADD TO BAG </button>
                    </div>
                </span>
            </div>

            <button class="shirt"> 
                <img src="/images/fatcat.png" alt="shirt" width="40" height="40"/>
                <p class="lyrics"> ITS FINE IT HAPPENS ALL THE TIME. </p>
            </button>
            <div class="content">
                <span class="flex">
                    <div class="holder">  
                        <div class="gallery">
                            <img src="/images/fatcat.png" onclick="myFunction3(this)" width="140" height="200"/>
                            <img src="/images/inside3.jpg" onclick="myFunction3(this)"/>
                            <img src="/images/inside2.jpg" onclick="myFunction3(this)"/>
                            <img src="/images/inside4.jpg" onclick="myFunction3(this)"/>
                        </div>
                    </div>
                    <div class="bigimage">
                        <img id="imageBox3" src="/images/inside4.jpg" />
                    </div>
                    <div class="texts">
                        <img class="random" src="/images/fatcat.png" hidden/>
                        <span class="title">now, tell me you DON'T want to have a capitalistic pig on ur back. omg jk! hes already there.</span>
                        <p> the front of this shirt is where the sublimal messages TRULY started. Somewhere on it, is trump, biden, the devil, angels and etc. 
                            <br> well maybe none of them are there and ur just crazy!!!!! [the back is those big piggy capitalistics...]
                        </p>
                        <p class="size"> made for L MEN. but gender isn't real so do with that as you will.</p>
                        <span class="thingPrice"> 25 </span>
                        <button class="checkout"> ADD TO BAG </button>
                    </div>
                </span>
            </div>

            <button class="shirt"> 
                <img src="/images/douvlepeople.png" alt="shirt" width="30" height="30"/>
                <p class="lyrics"> YOU MAKE COMES CRAWLING BACK TO YOU. </p> 
            </button>
            <div class="content">  
                <span class="flex">
                    <div class="holder">  
                        <div class="gallery">
                            <img src="/images/douvlepeople.png" onclick="myFunction4(this)"/>
                            <img src="/images/bbyt1.jpg" onclick="myFunction4(this)"/>
                            <img src="/images/bbytee3.jpg" onclick="myFunction4(this)"/>
                            <img src="/images/bbyt2.jpg" onclick="myFunction4(this)"/>
                        </div>
                    </div>
                    <div class="bigimage">
                        <img id="imageBox4" src="/images/bbyt1.jpg" />
                    </div>
                    <div class="texts">
                        <img class="random" src="/images/douvlepeople.png" hidden/>
                        <span class="title">the nightterrors logo showing off ur SEXI bod. jk only with it on are u sexi.</span>
                        <p> now this ribbed t is ur Y2K, brat summer, kamla harris kissing trump day DREAM. it comes in two sizes cause im not better then brandy mellvive. <br>
                        <br> the youth large, i'm 5'3, 130. the large should fit up to 6'2, 190 <br> if you need another size email me. im not trying to exclude.</p>
                        <select id="sizes" name="sizes" class="sizebutton">
                            <option value="YOUTH LARGE">YOUTH LARGE</option>
                            <option value="ADULT LARGE">ADULT LARGE</option>
                        </select>
                        <span class="thingPrice"> 15 </span>
                        <button class="checkout"> ADD TO BAG </button>
                    </div>
                </span>
            </div>

            <button class="shirt"> 
                <img src="/images/buckingbronc.png" alt="shirt" width="30" height="30"/>
                <p class="lyrics"> AND I HOPE THAT IT ALL SWALLOWS YOU. </p>
            </button>
            <div class="content">  
                <span class="flex">
                    <div class="holder">  
                        <div class="gallery">
                            <img src="/images/horserearing.png" onclick="myFunction5(this)" width="180" height="2"/>
                            <img src="/images/graycolared1.jpg" onclick="myFunction5(this)"/>
                            <img src="/images/graycollar3.jpg" onclick="myFunction5(this)"/>
                            <img src="/images/graycollared2.jpg" onclick="myFunction5(this)"/>
                        </div>
                    </div>
                    <div class="bigimage">
                        <img id="imageBox5" src="/images/graycolared1.jpg" />
                    </div>
                    <div class="texts">
                        <img class="random" src="/images/horserearing.png" hidden/>
                        <span class="title">this one is open, white space, lets the images BREATHE</span>
                        <p> are you a minimalist babe? you are!?!??!?! well this shirt is for YOU. it is minimal grpahics, left open to BREATHEEEEEEEEE. you will stun all the swed and danish humans with this SHIRT.</p>
                        <p class="size"> made for XL MEN. but gender isn't real so do with that as you will.</p>
                        <span class="thingPrice"> 30 </span>
                        <button class="checkout"> ADD TO BAG </button>
                    </div>
                </span>
            </div>

            <div class="contact">
                <p class="contacttitle"> Contact </p>  
                <p>     email - failennaselta@gmail.com <br>
                        instagram - n1ghtterrors
                    <br> hit my line if you have any questions, concerns or fears. 
                <br> </p>
                <p class="serect"> ill hold your hand and tell you what mom could never. (im proud of you.) </p>  
            </div>
        </div>

        <script>
        document.addEventListener('DOMContentLoaded', ready);

        function ready() {
            var removeCartItemButtons = document.getElementsByClassName('remove');
            for (var i = 0; i < removeCartItemButtons.length; i++) {
                var button = removeCartItemButtons[i];
                button.addEventListener('click', removeCartItem);
            }

            var quantityInputs = document.getElementsByClassName('cartInput');
            for (var i = 0; i < quantityInputs.length; i++) {
                var input = quantityInputs[i];
                input.addEventListener('change', quantityChanged);
            }

            var addToCartButtons = document.getElementsByClassName('checkout');
            for (var i = 0; i < addToCartButtons.length; i++) {
                var button = addToCartButtons[i];
                button.addEventListener('click', addToCartClicked);
            }

            var checkoutButton = document.getElementsByClassName('checkOut')[0];
            if (checkoutButton) {
                checkoutButton.addEventListener('click', processCheckout);
            }
        }

       
        var coll = document.getElementsByClassName("shirt");
        var i;

        const adjustContentHeight = (content) => {
            if (content.style.maxHeight && content.style.maxHeight !== "0px") {
                content.style.maxHeight = "0px"; 
            } else {
                content.style.maxHeight = content.scrollHeight + "px";
            }
        }

        for (i = 0; i < coll.length; i++) {
            
            coll[i].addEventListener("click", function() {
                this.classList.toggle("active");
                adjustContentHeight(this.nextElementSibling);
            }); 
        }


        window.addEventListener('load', function() {
            var coll = document.getElementsByClassName("shirt");
            for (var i = 0; i < coll.length; i++) {
                var content = coll[i].nextElementSibling;
                content.style.maxHeight = content.scrollHeight + "px";
            }
        });

  
        function myFunction(smallImg) {
            var fullImg = document.getElementById("imageBox");
            fullImg.src = smallImg.src;
        }

        function myFunction1(smallImg) {
            var fullImg = document.getElementById("imageBox1");
            fullImg.src = smallImg.src;
        }

        function myFunction2(smallImg) {
            var fullImg = document.getElementById("imageBox2");
            fullImg.src = smallImg.src;
        }

        function myFunction3(smallImg) {
            var fullImg = document.getElementById("imageBox3");
            fullImg.src = smallImg.src;
        }

        function myFunction4(smallImg) {
            var fullImg = document.getElementById("imageBox4");
            fullImg.src = smallImg.src;
        }

        function myFunction5(smallImg) {
            var fullImg = document.getElementById("imageBox5");
            fullImg.src = smallImg.src;
        }

        
        let openShopping = document.querySelector('.shopping');
        let closeShopping = document.querySelector('.closeShopping');
        let body = document.querySelector('body');

        openShopping.addEventListener('click', () => {
            body.classList.add('active');
        });

        closeShopping.addEventListener('click', () => {
            body.classList.remove('active');
        });

        function removeCartItem(event) {
            var buttonClicked = event.target;
            buttonClicked.parentElement.parentElement.remove();
            updateCartTotal();
            updateCartCounter();
        }

        function quantityChanged(event) {
            var input = event.target;
            if (isNaN(input.value) || input.value <= 0) {
                input.value = 1;
            }
            validateQuantity(input);
            updateCartTotal();
            updateCartCounter();
        } 

        function validateQuantity(input) {
            var cartItem = input.closest('.cartrow');
            var title = cartItem.querySelector('.shop-title').innerText.trim();
            var quantity = parseInt(input.value);


            fetch(`/check-inventory?title=${encodeURIComponent(title)}`)
                .then(response => response.json())
                .then(data => {
                    console.log('Inventory response:', data); 
                    var maxQuantity = data.inStock || 0;
                    if (quantity > maxQuantity) {
                        alert(`Sorry, only ${maxQuantity} of this item is available.`);
                        input.value = maxQuantity;
                        updateCartTotal();
                    }
                })
                .catch(error => {
                    console.error('Error checking inventory:', error);
                });
        }

        function addToCartClicked(event) {
            var button = event.target;
            var shopItem = button.parentElement;
            var title = shopItem.getElementsByClassName('title')[0].innerText.trim();
            var price = shopItem.getElementsByClassName('thingPrice')[0].innerText;
            var imageSrc = shopItem.getElementsByClassName('random')[0].src;

            var sizeDropdown = shopItem.querySelector('select');
            var selectedSize = sizeDropdown ? sizeDropdown.value : '';

            var displayTitle = selectedSize ? selectedSize + " - " + title : title;
            var lookupTitle = shortenTitle(displayTitle);

            console.log('Adding to cart - Display Title:', displayTitle); 
            console.log('Adding to cart - Lookup Title:', lookupTitle);
           
            fetch(`/check-inventory?title=${encodeURIComponent(lookupTitle)}`)
                .then(response => response.json())
                .then(data => {
                    console.log('Inventory check response:', data); 
                    if (data.inStock > 0) {
                        addItemToCart(displayTitle, price, imageSrc, 1, lookupTitle);
                        updateCartTotal();
                        updateCartCounter();
                    } else {
                        alert('Sorry, this item is sold out.');
                    }
                })
                .catch(error => {
                    console.error('Error checking inventory:', error);
                    alert('Error checking inventory. Please try again.');
                });
        }
        function addItemToCart(title, price, imageSrc, quantity, lookupTitle = null) {
        var cartItems = document.getElementsByClassName('cartItems')[0];
        var cartItemNames = cartItems.getElementsByClassName('shop-title');
        
       
        for (var i = 0; i < cartItemNames.length; i++) {
            if (cartItemNames[i].innerText.trim() === title) {
                
                var existingRow = cartItemNames[i].closest('.cartrow');
                var quantityInput = existingRow.querySelector('.cartInput');
                var currentQuantity = parseInt(quantityInput.value);
                var newQuantity = currentQuantity + quantity;
                
                
                fetch(`/check-inventory?title=${encodeURIComponent(lookupTitle || shortenTitle(title))}`)
                    .then(response => response.json())
                    .then(data => {
                        console.log('Inventory check response for existing item:', data);
                        if (newQuantity <= data.inStock) {
                            quantityInput.value = newQuantity;
                            updateCartTotal();
                            updateCartCounter();
                        } else {
                            alert(`Sorry, only ${data.inStock} of "${title}" is available. You already have ${currentQuantity} in your cart.`);
                        }
                    })
                    .catch(error => {
                        console.error('Error checking inventory:', error);
                        alert('Error checking inventory. Please try again.');
                    });
                return;
            }
        }

        
        var cartRow = document.createElement('div');
        cartRow.classList.add('cartrow');
        var cartRowContents = `
            <div class="itemsInCart">
                <img class="cart-item" src="${imageSrc}" width="100" height="100">
                <span class="shop-title">${title}</span>
            </div>
            <span class="priceboi">$${price}</span>
            <div class="cart-quanity">
                <input class="cartInput" type="number" value="${quantity}" min="1">
                <button class="remove">remove</button>
            </div>
        `;
        cartRow.innerHTML = cartRowContents;
        cartItems.append(cartRow);
        
        cartRow.getElementsByClassName("remove")[0].addEventListener('click', removeCartItem);
        cartRow.getElementsByClassName('cartInput')[0].addEventListener('change', quantityChanged);
        updateCartCounter();
    }

        function processCheckout(event) {
            event.preventDefault();
            
            var cartRows = document.querySelectorAll('.cartItems .cartrow');
            if (cartRows.length === 0) {
                alert('Your cart is empty!');
                return;
            }

            var items = [];
            
            cartRows.forEach(cartRow => {
                var title_wa = cartRow.querySelector('.shop-title').innerText.trim();
                var priceText = cartRow.querySelector('.priceboi').innerText.replace('$', '').trim();
                var price_wa = parseFloat(priceText);
                var quantity_wa = parseInt(cartRow.querySelector('.cartInput').value);
                var imageUrl = cartRow.querySelector('.cart-item').src;
                
                items.push({
                    title_wa: shortenTitle(title_wa),
                    price_wa: price_wa,
                    quantity_wa: quantity_wa,
                    imageUrl: imageUrl
                });
            });

            console.log('Processing checkout with items:', items);
            
            fetch('/checkout', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ items: items })
            })
            .then(response => {
                if (!response.ok) {
                    return response.json().then(err => Promise.reject(err));
                }
                return response.json();
            })
            .then(data => {
                if (data.url) {
                    window.location.href = data.url;
                } else {
                    throw new Error('No checkout URL received');
                }
            })
            .catch(error => {
                console.error('Checkout Error:', error);
                alert(error.error || 'Checkout failed. Please try again.');
            });
        }

        function updateCartTotal() {
            var cartItemContainer = document.getElementsByClassName('cartItems')[0];
            var cartRows = cartItemContainer.getElementsByClassName('cartrow');
            var total = 0;

            for (var i = 0; i < cartRows.length; i++) {
                var cartRow = cartRows[i];
                var priceElement = cartRow.getElementsByClassName('priceboi')[0];
                var quantityElement = cartRow.getElementsByClassName('cartInput')[0];

                var price = parseFloat(priceElement.innerText.replace('$', ''));
                var quantity = parseInt(quantityElement.value) || 1;

                var subtotal = price * quantity;
                total += subtotal;
            }

            total = Math.round(total * 100) / 100;
            document.getElementsByClassName('totalPrice')[0].innerText = '$' + total;
        }

        if (document.readyState == 'loading') {
            document.addEventListener('DOMContentLoaded', ready);
        } else {
            ready();
        }

        function shortenTitle(displayTitle, maxLength = 15) {
        if (!displayTitle) return '';
        
        if (displayTitle.length <= maxLength) {
            return displayTitle;
        }
        
        
        const truncated = displayTitle.substring(0, maxLength);
        const lastSpaceIndex = truncated.lastIndexOf(' ');
        
        
        if (lastSpaceIndex > maxLength * 0.7) {
            return truncated.substring(0, lastSpaceIndex).trim();
        }
        
       
        return truncated.trim();
}

        function updateCartCounter() {
        var cartRows = document.querySelectorAll('.cartItems .cartrow');
        var totalItems = 0;
        
        cartRows.forEach(cartRow => {
            var quantityInput = cartRow.querySelector('.cartInput');
            totalItems += parseInt(quantityInput.value) || 0;
        });
        
        var counter = document.getElementById('cartCounter');
        counter.textContent = totalItems;
            }
        let titleTriggered = false;
        let hasTriggered = false;

        function handleScroll() {
            const scrollPosition = window.scrollY;
            const menuTriggerPoint = window.innerHeight * 2;
            const titleTriggerPoint = window.innerHeight * 0.5;
            
            
            if (scrollPosition > menuTriggerPoint && !hasTriggered) {
            const menuElement = document.getElementById('diagonalMenu');
            menuElement.classList.add('active');

            setTimeout(() => {
            menuElement.classList.add('typed');
            }, 4000);

            hasTriggered = true;

        }
            
            
            if (scrollPosition > titleTriggerPoint && !titleTriggered) {
                const titleElement = document.getElementById('titleElement');
                titleElement.classList.add('active');
                titleTriggered = true;
                
                setTimeout(() => {
                    titleElement.classList.add('phase-3');
                }, 6000);
            }
        }
        

        
        window.addEventListener('scroll', handleScroll);

        document.getElementById('diagonalMenu').addEventListener('click', function() {
            this.classList.toggle('centered');
        });

        
        document.addEventListener('click', function(e) {
            if (!e.target.closest('.diagonal-menu')) {
                document.getElementById('diagonalMenu').classList.remove('centered');
            }
        });

        window.addEventListener('load', function() {
            const video = document.getElementById('myVideo');
            video.play().catch(e => {
                console.log('Autoplay failed:', e);
            });
        });
        </script>
    </body>
</html>